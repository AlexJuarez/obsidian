define(function(require) {
    'use strict';

    require('./apiURI');
    require('./defaultApiVersion');
    require('./creativeSettings');
    require('./enums');
    require('./urlRegex');
    require('./moneyRegex');
    require('./isMobile');
    require('./dateFormat');
});
