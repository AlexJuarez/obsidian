define(function(require) {
    'use strict';

    require('./apiURI');
    require('./creativeSettings');
    require('./enums');
    require('./urlRegex');
    require('./moneyRegex');
    require('./isMobile');
    require('./dateFormat');
});
